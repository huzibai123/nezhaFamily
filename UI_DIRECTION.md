# 哪吒家庭 UI 方向说明

## 一句话方向

哪吒家庭不是一个「黑色极简图片流」，而是一个私有的家庭记忆中枢。

移动端可以像家庭朋友圈，快速发布、浏览、互动；PC 端应该像家庭资料室和记忆工作台，能同时看到时间线、相册、日历、成员和最近互动。

## 当前问题判断

当前 UI 的主要问题不是样式细节，而是信息架构偏了：

- PC 端大量页面使用 `max-w-[640px]` 或 `max-w-[380px]`，导致桌面上只有中间一条窄内容流。
- 首页只有时间线，没有形成家庭平台的上下文。
- 相册、日历、个人页虽然存在，但像分散页面，没有在首屏共同构成「家庭记忆系统」。
- 纯黑极简风格放大了空旷感，让产品像通用模板，而不是属于一个家庭的空间。
- 目前页面更像「移动端稿居中显示」，不是桌面端响应式设计。

## 产品气质

目标气质：

- 温暖，但不幼稚。
- 安静，但不空。
- 私密，但不封闭。
- 有照片感、日期感、人物关系感。
- 更像「家里的记忆册」，少一点「作品集式极简」。

避免方向：

- 不做营销落地页。
- 不做纯黑大留白的视觉展示页。
- 不做只有卡片和按钮的空壳后台。
- 不用过多渐变、玻璃拟态、装饰光斑。
- 不把所有页面都限制成手机宽度。

## PC 端信息架构

PC 端建议采用三栏应用壳：

### 左侧导航

固定宽度，承担「家庭空间」的骨架：

- 家庭名称
- 当前用户
- 时间线
- 相册
- 日历
- 家庭成员
- 设置/邀请成员

### 中间主内容

根据页面变化：

- 首页：时间线流
- 相册：照片网格/相册集合
- 日历：月视图或周视图
- 帖子详情：媒体与评论
- 发布页：编辑器与上传区

中间内容不应固定为 640px。建议在桌面端使用 720px 到 860px 的主栏宽度，并允许右侧栏参与布局。

### 右侧上下文栏

这是 PC 端变得「不像手机放大版」的关键：

- 今日/本周家庭事件
- 最近评论
- 最近上传的照片
- 家庭成员列表
- 快速发布入口
- 相册快捷入口
- 数据概览：帖子数、照片数、成员数、本月事件数

右侧栏不一定每页都有，但首页必须有。

## 移动端信息架构

移动端继续保持轻量：

- 顶部：家庭名称 + 发布按钮
- 主体：单列时间线
- 底部或顶部入口：时间线、相册、日历、我的
- 发布流程尽量一步完成

移动端重点是「拍完立刻发」，不是复杂管理。

## 首页改版建议

首页应该从单列 timeline 改成「家庭今日概览 + 时间线」。

首屏建议包含：

- 左侧导航：家庭空间结构
- 中间：最近动态，按日期或帖子卡展示
- 右侧：本月事件、最近评论、家庭成员

时间线卡片应补足家庭语境：

- 作者头像或首字
- 家庭角色，例如 爸爸/妈妈/宝宝
- 日期分组，例如 今天、昨天、6 月 8 日
- 媒体数量
- 评论摘要，而不只是评论数量

## 视觉语言

### 颜色

不要继续纯黑一把梭。建议使用深色暖灰作为基础：

- 页面背景：接近黑但带一点暖度
- 面板背景：深灰
- 边框：低对比
- 强调色：温暖红、柔和橙或家庭品牌色
- 照片本身作为主要色彩来源

### 圆角

控制在 8px 到 12px 为主。照片卡可以 10px 或 12px。不要所有东西都胶囊化。

### 字体

桌面端减少过大的标题，增加信息层级：

- 页面标题不要占太多空间
- 卡片正文要可读
- 日期、评论、人物关系要清晰

### 图标

使用 `lucide-vue-next`，替代文本符号和 emoji。

例如：

- Heart / MessageCircle
- CalendarDays
- Images
- Users
- Plus
- LogOut
- Settings

## 页面级方向

### 登录/注册

现在登录页太空。建议加入左侧或背景区域：

- 家庭照片拼贴
- 产品说明：私有部署、仅家庭成员、照片/视频/评论
- 初始化状态：管理员登录、邀请码注册

但不要做营销 hero。它仍然是登录页，只是要有「这是家庭空间」的证据。

### 时间线

桌面端不要只做 640px 单列。建议：

- 日期分组
- 中栏帖子
- 右栏上下文
- 顶部快速发布条

### 发布页

发布页应更像编辑器：

- 左侧/上方：文字输入
- 右侧/下方：媒体预览
- 上传状态明确
- 支持纯文字、纯图、图文

当前「必须有文字才能发布」不适合家庭照片场景，纯图也应该允许。

### 相册

相册页适合 PC 做大网格：

- 相册集合
- 最近照片瀑布流
- 按年月筛选
- 空相册要提示如何添加照片

### 日历

日历在 PC 端可以更宽：

- 月历主区域
- 右侧事件列表
- 事件类型筛选

当前 640px 月历在桌面上太窄。

### 帖子详情

桌面端建议左右布局：

- 左侧：媒体浏览
- 右侧：作者信息、正文、点赞、评论输入、评论列表

移动端再恢复上下结构。

## 工程落地顺序

1. 修复当前构建问题，确保 `npm run build` 可通过。
2. 创建基础布局组件：
   - `AppShell.vue`
   - `DesktopSidebar.vue`
   - `RightRail.vue`
   - `MobileTopNav.vue`
3. 重做首页桌面布局，不先改所有页面。
4. 为首页补右侧上下文数据：
   - 本月事件
   - 最近相册
   - 家庭成员
   - 最近评论
5. 再逐页改造：
   - 帖子详情
   - 发布页
   - 相册页
   - 日历页
   - 个人页
6. 最后统一视觉 token，删除旧的重复样式文件。

## 验收标准

PC 端：

- 1280px 宽度下不应只有中间一条 640px 内容。
- 首页首屏至少有两个以上信息区块，而不是只有帖子流。
- 用户能在首页看出：这是家庭平台，不是普通图片流。
- 导航、相册、日历、发布、成员关系要同时可感知。

移动端：

- 发布时间不超过三步。
- 时间线单列清晰。
- 相册/日历入口容易到达。
- 不牺牲微信浏览器可用性。

工程：

- `npm run build` 通过。
- 后端测试不应卡住。
- 页面不依赖硬编码 localhost。
- 文档状态与真实代码状态一致。

## 暂不做

- 不做 AI 相册分类。
- 不做复杂动效。
- 不做公开分享。
- 不做多家庭租户。
- 不做大型后台管理台。

先把「家庭记忆中枢」这个产品感做出来。
